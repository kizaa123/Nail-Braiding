import ProfessionalSectionPage from '../_section';

export default function Page() {
  return (
    <ProfessionalSectionPage title="Settings" body="Session, notification preferences, and WhatsApp contact live behind the same role-guarded API." />
  );
}
