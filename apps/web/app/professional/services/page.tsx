import ProfessionalSectionPage from '../_section';

export default function Page() {
  return (
    <ProfessionalSectionPage
      title="Services"
      body="Add services with server-side prices and durations. Clients cannot override these values at booking time."
    />
  );
}
