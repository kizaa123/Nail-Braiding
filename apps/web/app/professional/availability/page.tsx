import ProfessionalSectionPage from '../_section';

export default function Page() {
  return (
    <ProfessionalSectionPage
      title="Availability"
      body="Set weekly hours. The booking engine rejects overlapping PENDING and CONFIRMED appointments."
    />
  );
}
