import ProfessionalSectionPage from '../_section';

export default function Page() {
  return (
    <ProfessionalSectionPage
      title="Portfolio"
      body="Upload JPEG, PNG, or WebP images. Files are stored in Cloudinary (or local-dev storage) — never in PostgreSQL."
    />
  );
}
